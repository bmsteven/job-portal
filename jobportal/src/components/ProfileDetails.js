import React from 'react'
import { View, Image } from "react-native"

const ProfileDetails = ({image, name, location}) => {
    return (
        <View>
            
        </View>
    )
}

export default ProfileDetails
